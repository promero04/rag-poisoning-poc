# RAG Poisoning PoC — Dependencias
# Proyecto SDI — Seguridad de la Información — Deusto 2025/26
# Pablo García
#
# Probado con Python 3.13 (Homebrew). Instalar torch CPU-only ANTES de
# este fichero:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Luego:
#   pip install -r requirements.txt
#
# Las versiones usan rangos compatibles, no pins exactos, para que el
# resolver de pip elija lo ultimo compatible sin romper APIs.

# ── Core RAG ──────────────────────────────────────────────────────────────────
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0,<0.4.0

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb>=0.5.0,<1.0.0
langchain-chroma>=0.1.4,<0.3.0

# ── Embeddings (local, sin API) ───────────────────────────────────────────────
sentence-transformers>=3.0,<6.0
langchain-huggingface>=0.1.0,<0.3.0

# ── LLM: Ollama (local) + OpenAI (fallback) ───────────────────────────────────
langchain-ollama>=0.2.0,<0.4.0
langchain-openai>=0.2.0,<0.4.0       # Solo si se tiene API key

# ── Utilidades ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
colorama>=0.4.6                 # Colores en terminal para la demo
tabulate>=0.9.0                 # Tablas en los resultados
pyyaml>=6.0                     # Carga de datasets (queries, indicadores)
matplotlib>=3.8                 # Graficos de resultados (metricas.py --plots)
